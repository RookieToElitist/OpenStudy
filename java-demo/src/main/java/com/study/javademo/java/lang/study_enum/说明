枚举类：其对象必须是可以穷举的。一般对象为常量时，建议使用枚举类.

定义枚举类的方式有两种：
    （1）jdk5.0之前，声明一个类，给定私有属性，提供私有有参构造方法，提供公开的静态常量对象并利用构造器初始化，这就可以使用了
    （2）jdk5.0之前，使用enum关键字定义枚举类（推荐）

在enum修饰的类可以实现接口，分两种形式：
    （1）当前枚举类重写接口方法，则枚举类中所有对象返回的信息是一样的
    （2）让枚举对象重写接口方法，则每个对象返回的信息都可以不一样